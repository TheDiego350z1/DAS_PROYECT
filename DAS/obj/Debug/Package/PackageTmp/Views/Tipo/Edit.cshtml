@model DAS.Models.TipoViewModel.EditTipoViewModel
@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

<div class="row">
    <div class="col-md-12">
        @using (Html.BeginForm("Edit", "Tipo", FormMethod.Post))
        {
            @Html.AntiForgeryToken()

            @Html.HiddenFor(d => d.Id)
            @Html.ValidationMessage("error", new { @class = "text-danger" })

            @Html.LabelFor(d => d.Name)
            @Html.TextBoxFor(d => d.Name, new { @class = "form-control form-control-lg" })
            @Html.ValidationMessage("Name", new { @class = "text-danger" })
            <br />

            <div>
                <input type="button" onclick="document.location.href='@Url.Content("~/User/ShowUser")'" class="btn btn-danger btn-lg" value="Cancenar" />
                <input type="submit" class="btn btn-success  btn-lg" value="Guardar" />
            </div>
        }
    </div>
</div>